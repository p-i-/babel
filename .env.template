# babel — local secrets template
# Copy this file to .env and fill in your key(s). NEVER commit .env itself.

# Required — get a free key at https://aistudio.google.com/apikey
GEMINI_API_KEY=

# Optional — enables in-lesson image search (helpers.search_images). Leave blank to skip.
SEARLO_API_KEY=

# Optional — DEV_MODE=dev makes the teacher surface operational glitches loudly
# (for development). Leave unset for normal tutoring behaviour.
#DEV_MODE=
